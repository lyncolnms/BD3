<% include ../header %>

<div class="wrapper">
	<div class="col-xs-9">
		<table class="table table-hover">
			<thead>
				<tr>
					<th>Usuário</th>
					<th>Senha</th>
				</tr>
			</thead>
			<tbody>
				<% for(x in users)  { %>
					<tr><%= x.usuario %></tr>
					<tr><%= x.senha %></tr>
				<% } %>
			</tbody>
		</table>
	</div>
</div>

<% include ../footer %>